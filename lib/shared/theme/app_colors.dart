import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary Colors
  static const Color primary = Color(0xFFFDB022);
  static const Color primaryLight = Color(0xFFFFE082);
  static const Color primaryDark = Color(0xFFF57C00);

  // Backgrounds
  static const Color backgroundLight = Color(0xFFE8F5F7);
  static const Color cardBackground = Color(0xFFFAF8F6);
  static const Color white = Color(0xFFFFFFFF);

  // Text
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textHint = Color(0xFFBDBDBD);

  // Borders
  static const Color border = Color(0xFFE0E0E0);
  static const Color borderFocused = Color(0xFFFDB022);

  // Status
  static const Color error = Color(0xFFF44336);
  static const Color success = Color(0xFF4CAF50);
}
